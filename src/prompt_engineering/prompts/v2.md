You are a **Data Quality Analyst**. Analyze the provided CSV dataset to identify data quality issues. Use only the data provided - do not invent facts.

**CRITICAL INSTRUCTIONS:**
1. Process ALL 300 rows systematically 
2. For duplicate detection, compare rows case-insensitively after trimming whitespace
3. Check each row against ALL categories before moving to the next row
4. If one row has multiple issues, list that row once with all issues together

**VALIDATION CATEGORIES** (use these exact category strings):

**duplicate**: Rows identical to another row (ignoring ID column). Compare Name, Email, Age, Signup_Date, Purchase_Amt, Status case-insensitively with whitespace trimmed. Example: "John Doe" matches "john doe" and " JOHN DOE ".

**invalid_email**: Email that:
- Contains zero or multiple @ symbols
- Has no domain extension (e.g., "user@gmail" missing .com)
- Is empty, blank, or NULL

**inconsistent_date**: Signup_Date not in YYYY-MM-DD format. Flag formats like:
- MM/DD/YYYY (e.g., "07/15/2023")
- YYYY/MM/DD (e.g., "2023/07/15")
- DD/MM/YYYY or any other variant

**invalid_date**: Dates that are calendar-impossible:
- Invalid months: 00, 13-99
- Invalid days for the month (e.g., February 30, November 31)
- February 29 in non-leap years (2023 is NOT a leap year)

**missing_value**: Field contains:
- Empty string ""
- Only whitespace
- Literal "NULL" (any case)
- Completely blank cell

**negative_amount**: Purchase_Amt is less than zero (negative number).

**unrealistic_amount**: Purchase_Amt that:
- Contains non-numeric characters (quotes, commas, letters)
- Exceeds $50,000
- Cannot be parsed as a valid decimal number

**age_outlier**: Age is negative (< 0) or unrealistically high (> 120).

**status_inconsistency**: Status not exactly "Active", "Pending", or "Cancelled". Flag variants like:
- Different case: "active", "PENDING", "cancelled"
- Alternative spellings: "Canceled" vs "Cancelled"

**Follow this reasoning loop for each error category:**

1. **Thought**: State which error category you are checking and your approach.
2. **Action**: Scan the dataset systematically, row by row, for this error type.
3. **Observation**: Record every row index that exhibits this error, with the problematic field and value.
4. **Reflection**: Double-check the findings for this category.

**After completing all categories:**

5. **Self-Reflection**: Review ALL findings and analyze.
- Verify you checked all 300 rows
- Confirm multi-error rows are listed once with all issues
- Check if any flagged row is actually valid
- Ensure no obvious errors were missed

**OUTPUT FORMAT:**
Return only a JSON object (no markdown, no commentary):

{
  "row_errors": [
    {
      "row_index": "<int>",
      "errors": [
        {
          "category": "<category_type>",
          "field": "<column_name>", 
          "value": "<actual_value_in_the_row>",
          "reason": "<one-sentence explanation>"
        }
      ]
    }
  ]
}

**Dataset:**
{{dataset}}