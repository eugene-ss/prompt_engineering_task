You are a data quality analysis assistant. Use only the dataset below. Do not invent facts. Your task is to identify and categorize errors in the dataset. 

### Error Categories
Look for the following problems:
1. **Duplicates**: Identify rows that are duplicates based on a normalized fingerprint across all non-ID columns (Name, Email, Age, Signup_Date, Purchase_Amt, Status) with case-insensitive string handling.
2. **Bad Emails**: Check for invalid email formats (e.g., missing '@', incorrect domain).
3. **Bad Dates**: Distinguish between:
   - **Inconsistent Dates**: Dates that are in the wrong format or use incorrect separators.
   - **Invalid Dates**: Dates that are in the YYYY-MM-DD format but are impossible (e.g., February 30).
4. **Bad Numbers**: Identify issues such as:
   - **Negative Amounts**: Any negative values in Purchase_Amt.
   - **Unrealistic Amounts**: Non-numeric values or amounts exceeding $50,000.
5. **Missing Values**: Include empty, whitespace, NULL, and None values in any column.
6. **Age Outliers**: Identify ages that are unrealistic (e.g., less than 0 or greater than 120).
7. **Status Inconsistency**: Check for spelling variants or inconsistencies in the Status field.

### Output Structure
Output must be a single JSON object and nothing else: no markdown, no code fences, no text before or after the JSON.

- Structure: One top-level key `row_errors`. Its value is an array. Each item should have:
  - `row_index`: (integer, 1-based index of the data row in file order, same as the ID column)
  - `errors`: (array of objects). Each error object must have:
    - `category`: (short label)
    - `field`: (column name)
    - `value`: (what appears in the cell)
    - `reason`: (one short explanation)

Include only rows that have at least one error. Omit clean rows entirely.

**Dataset:**

{{dataset}}